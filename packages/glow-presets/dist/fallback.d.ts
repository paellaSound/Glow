import { FALLBACK_PRESET_ID } from './constants.js';
export { FALLBACK_PRESET_ID };
export declare function computeFallbackColor(roomCode: string, seedTimestamp: number, row: number, col: number, now: number): string;
